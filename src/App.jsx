import React from 'react';
import TimerView from './components/TimerView';

function App() {
  return (
    // The #root in CSS handles the height now, so we just render the view
    <TimerView />
  );
}

export default App;